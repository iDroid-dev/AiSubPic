<!DOCTYPE html>
<html lang="ru" data-theme="light">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title || 'Admin Panel' }}</title>
  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body>

  {{-- Компонент Drawer из DaisyUI --}}
  <div class="drawer lg:drawer-open">
    <input id="my-drawer-2" type="checkbox" class="drawer-toggle" />
    
    {{-- ОСНОВНОЙ КОНТЕНТ --}}
    <div class="drawer-content flex flex-col bg-base-200 min-h-screen">
      
      {{-- Верхняя панель (Navbar) для мобильных --}}
      <div class="w-full navbar bg-base-100 lg:hidden shadow-sm">
        <div class="flex-none">
          <label for="my-drawer-2" class="btn btn-square btn-ghost">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="inline-block w-6 h-6 stroke-current"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
          </label>
        </div>
        <div class="flex-1 px-2 mx-2 text-xl font-bold">Admin Panel</div>
      </div>

      {{-- Сюда подставляется контент страниц (Dashboard, Bots и т.д.) --}}
      <div class="p-8">
        {{-- Вывод Flash сообщений (Успех/Ошибка) --}}
        @if(flashMessages.has('success'))
          <div class="alert alert-success mb-6">
            <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
            <span>{{ flashMessages.get('success') }}</span>
          </div>
        @endif
        
        @if(flashMessages.has('error'))
          <div class="alert alert-error mb-6">
            <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
            <span>{{ flashMessages.get('error') }}</span>
          </div>
        @endif

        {{{ await $slots.main() }}}
      </div>
    
    </div> 
    
    {{-- БОКОВОЕ МЕНЮ (SIDEBAR) --}}
    <div class="drawer-side z-20">
      <label for="my-drawer-2" aria-label="close sidebar" class="drawer-overlay"></label> 
      <ul class="menu p-4 w-80 min-h-full bg-base-100 text-base-content shadow-xl">
        {{-- Логотип / Заголовок --}}
        <li class="mb-6">
            <div class="flex items-center gap-4 px-2">
                <div class="avatar placeholder">
                    <div class="bg-neutral text-neutral-content rounded-full w-10">
                      <span class="text-xl">AI</span>
                    </div>
                </div> 
                <span class="text-xl font-bold">Super Admin</span>
            </div>
        </li>

        {{-- ССЫЛКИ МЕНЮ --}}
        
        <li>
            <a href="{{ route('admin.dashboard') }}" class="{{ request.matchesRoute('admin.dashboard') ? 'active' : '' }}">
                 📊 Дашборд
            </a>
        </li>

        <li>
            <a href="{{ route('admin.bots.index') }}" class="{{ request.url().startsWith('/admin/bots') ? 'active' : '' }}">
                 🤖 Мои Боты
            </a>
        </li>

        <li>
            <a href="{{ route('admin.plans.index') }}" class="{{ request.url().startsWith('/admin/plans') ? 'active' : '' }}">
                💎 Тарифы и Цены
            </a>
        </li>

        <li>
          <a href="{{ route('admin.models.index') }}" class="{{ request.url().startsWith('/admin/models') ? 'active' : '' }}">
            🧠 AI Модели
          </a>
        </li>

        {{-- ПЛАТЕЖИ --}}
        <li>
          <a href="{{ route('admin.payments.index') }}" class="{{ request.url().startsWith('/admin/payments') ? 'active' : '' }}">
            💰 Платежи
          </a>
        </li>

        <li>
            <a href="{{ route('admin.users.index') }}" class="{{ request.url().startsWith('/admin/users') ? 'active' : '' }}">
                 👥 Пользователи
            </a>
        </li>

        <li>
          <a href="{{ route('admin.mailing.index') }}" class="{{ request.url().startsWith('/admin/mailing') ? 'active' : '' }}">
            📢 Рассылка
          </a>
        </li>
        
        <div class="divider"></div>
 
        <li>
            <a href="/logout" class="text-error">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" /></svg>
                Выйти
            </a>
        </li>
      </ul>
    </div>
  </div>

</body>
</html>